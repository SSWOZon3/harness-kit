{
  "name": "harnesskit",
  "version": "0.1.0",
  "private": true,
  "description": "Local agentic CLI for turning repositories into AI-ready workspaces.",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "typecheck": "pnpm build && pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^22.10.2",
    "fs-extra": "^11.2.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.9"
}
